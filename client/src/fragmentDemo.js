import React from 'react';

function FragmentDemo() {
    return(
        //instead of <React.Fragment> we can use <> and </> 
        // it basicaly reprsents 
        <React.Fragment>  
        <h1>Fragment Demo</h1>
        <p>Fragment paragraph</p>
        </React.Fragment>
    )
}

export default FragmentDemo;